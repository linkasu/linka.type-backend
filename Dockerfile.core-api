FROM golang:1.24.5 AS build

WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o /out/core-api ./cmd/core-api

FROM gcr.io/distroless/base-debian12
COPY --from=build /out/core-api /core-api
ENV PORT=8080
ENTRYPOINT ["/core-api"]
