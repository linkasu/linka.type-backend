package db

import "linka.type-backend/models"

// Re-export types from models for backward compatibility
type User = models.User
type Category = models.Category
type Statement = models.Statement
type OTPCode = models.OTPCode